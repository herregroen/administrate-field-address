<%#
# Address Form Partial
This partial renders an input element for a string attribute.
By default, the input is a Google Maps Places Autocomplete field.
## Local variables:
- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of Administrate::Field::Address.
  A wrapper around the Address pulled from the database.
%>

<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <%= f.text_field field.attribute, class: "address-input" %>
  <div class="address-input-map"></div>
</div>
